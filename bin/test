#!/usr/bin/env bash

multi_bin=/tmp/buildpacks/custom/bin

if [[ -f $1/buildpack-multi-test.sh ]]; then
  source $1/buildpack-multi-test.sh
  exit $?
else
  echo "buildpack-multi-test.sh not found"
  exit 1
fi
